GLOBAL BUILD CONSTRAINTS — ASORA * Inventory-only system. No work orders, tickets, dispatch, scheduling, labor, job costing, or CMMS features. * Inventory truth is ledger-derived only (append-only, immutable). * No mutation of ledger events after commit. * All writes are tenant-scoped and audited. * All reads are reproducible from authoritative facts. * RBAC (B11) enforced before plan gating (B12). * Plan gating enforced before feature execution. * Alerts, integrations, exports are observer-only. * Mobile offline never mutates server truth. * No implicit automation. All state changes are explicit. * UTC everywhere. * Determinism over convenience. * Fail-closed on ambiguity. * Spec threads are authoritative over memory or assumptions. Any request violating these constraints must be rejected.
